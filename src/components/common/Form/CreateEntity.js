import React from 'react';
import Dropdown from '../Dropdown';
import Field from './Field';

const CreateEntity = ({fields, dependencies = []}) => {
  return (
    <div>CreateEntity</div>
  )
}

export default CreateEntity